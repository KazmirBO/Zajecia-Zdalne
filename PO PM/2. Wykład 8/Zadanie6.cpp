#include <algorithm>
#include <array>
#include <cmath>
#include <functional>
#include <iostream>
#include <numeric>
#include <vector>
#define nl std::cout << std::endl
int main(int argc, char const *argv[]) {
  std::vector<int> v1(4, 3);
  std::vector<int> v2(2, 5);
  int a = std::inner_product();
  std::cout << "cos alfa = " < < < < std::endl;
  std::cout << "alfa = " < < < < std::endl;
  return 0;
}
